import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kisaragihiu.kemdict',
  appName: 'Kemdict',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
