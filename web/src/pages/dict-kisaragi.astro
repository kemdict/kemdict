---
import MarkdownLayout from "$src/MarkdownLayout.astro";
import WordList from "$src/components/WordList.astro";
import { api } from "$src/common";
const frontmatter = {
  title: "字典 | 如月的現代台灣華語補足典",
};
const titles = await api(`/titles/kisaragi_dict`);
---

<MarkdownLayout frontmatter={frontmatter}>
  <h1>字典 — 如月的現代台灣華語補足典</h1>
  <p>我記錄台灣華語有在使用但沒被收錄的詞的地方。</p>
  <p>目前共有 {titles.length} 條：</p>
  <WordList words={titles} />
</MarkdownLayout>
