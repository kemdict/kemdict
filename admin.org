* Adding a dictionary

See commit [[https://github.com/kemdict/kemdict/commit/7c1a703][7c1a703]] for an example.

- dicts
  - add code to Makefile to download the raw data at build time
  - update process-data.el:
    - declare the new dictionary
    - linkify fields if needed
    - consider which aliases need to be added
    - maybe index tags
  - fill in info in data.ts and maybe README
- web
  - most likely, write a new word component for the new dictionary
  - Update _Word.svelte to dispatch word rendering to that new component
  - Make sure the heading linking style is correct (declared in data.ts)
  - Include copyright information in word footer

* Testing
- Does search work?
  - "a", "a p" (prefix, suffix), "無妨", "zzz", "一"
  - Language tabs
  - Pagination
- Does word page rendering work?
  - "無妨" (taijit, concised, revised, =moedict_twblg=)
  - "實聯制" (kisaragi, itaigi)
- About
- Initial index + pagination, Radical index

* Why not make the DB its own service

I'm still not sure, but

- The network latency might be worse than running DB queries regardless of caching
- More attack vectors (?)
- Another interface to maintain within the same app
